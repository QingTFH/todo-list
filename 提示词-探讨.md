## 默认要求
0. 在必要时，联网搜索是被允许的
1. 输出内容不要有套话
2. 可以使用任何你擅长的语言思考, 但要求使用中文回答

## 探讨内容
query进行查询后，我往往会从上到下依次进行；但目前的query功能只按照日期进行排列，而生活中我往往会优先完成更重要的事项，这也契合了四象限任务分类法：按照重要+紧急两个维度给事件排序；
在程序中，如果要落地这个排序方法，我们需要考虑两个问题：
1. 如何合适的安排”重要程度“？是给一个参数，按0~10进行打分；还是预先设定五个等级，例如"不重要"~”重要“；或是其他更好的方法？
2. 如何给紧急和重要加权：两个维度需要共同考虑，这意味着最终优先级是两个维度加权得到的，那么这个权重应该如何确定呢？例如，当前权重 = 1 * 剩余日期；加入重要维度后，会变成 a * 剩余日期 + b * 紧急程度(只是一个例子，不一定真的使用这个计算方式)，那么，a和b要如何确定？