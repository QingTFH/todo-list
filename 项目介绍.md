# 项目介绍

一个纯 Java 编写的命令行待办事项（Todo List）程序。用户通过控制台输入指令，对待办进行增、查、完成、编辑、删除操作，数据以纯文本文件持久化（待办 `todo.txt`、已完成事项 `finish.txt`）。

## 技术要点

- 语言：Java（未使用任何第三方库，无 Maven/Gradle 构建配置，编译产物在 `out/production`）
- 运行方式：启动 `src/main/Main.java` 进入交互式命令行，输入指令操作
- 持久化：明文文件 `todo.txt`（待办）与 `finish.txt`（已完成），一行一条，格式 `pri:x; ddl:yyyy-MM-dd HH:mm; content:内容`（`pri` 缺省按 1 解析，兼容旧文件）

## 架构

严格遵循五层单向依赖，不跨层调用：

```
Input（输入器）→ Controller（调度器）→ Util / Manager（工具箱 / 数据管理器）→ Dao（数据小助手）→ 文件
```

数据流：

- 启动：Dao 从硬盘读入内存 → TodoManager
- 运行：Input 解析指令 → Controller 调度 → Handler 执行 → TodoManager 改内存数据
- 数据改动：TodoManager 即时落盘 → Dao 写回文件

## 已实现功能（指令）

| 指令 | 说明 | 示例 |
| --- | --- | --- |
| `stop` | 退出程序，退出前保存 | `stop` |
| `add` | 新增待办 | `add -m 交实验报告 -d 2026-05-20 -h 17:00 -i 2` |
| `query` | 查询待办：**已逾期**单独分区、按 ddl 升序且不计算 score；**未逾期**按重要×紧急 score 降序；`content; ddl` 定宽列对齐，默认不显示 `pri`/`score`；加 `-d`（或 `-detail`/`--detail`）额外显示 `pri` 与 `score`；`-f` 查询已完成事项（默认前 10 条，`-a` 全部，`-n` 前 n 条） | `query` / `query -d` / `query -n 3` / `query -f` |
| `finish` | 完成第 n 条，移出待办并写入 finish.txt；`-o` 一键完成所有已逾期任务 | `finish 2` 或 `finish -n 2` / `finish -o` |
| `edit` | 修改第 n 条的内容/ddl/重要度（缺省项保留原值） | `edit 2 -c 新内容 -d 2026-06-20 -i 3` |
| `delete` | 彻底删除第 n 条（不进 finish.txt）；`-o` 一键删除所有已逾期任务 | `delete 2` 或 `delete -n 2` / `delete -o` |
| `help` | 显示所有指令；`help 指令名`（或 `help -指令名`）显示该指令参数及作用 | `help` / `help add` |

指令采用 Linux 风格：`指令 [选项] 内容`。选项支持短名（`-m`）与长名（`--message`/`-message`，经 `Command.normalize` 归一到短名），`-` 开头的 token 视为选项，其余视为裸参数；未知指令会提示"请输入 help 查看可用指令"。

## 目录结构

```
src/
├── main/          入口（Main）与全局配置（Config：文件路径、时间格式、调试开关）
├── io/
│   ├── input/     输入循环与指令解析入口
│   └── output/    Output（用户信息）与 DebugOutput（受 Config.debug 开关控制的调试日志）
├── controller/    调度器，把指令分发给对应 Handler
├── token/
│   ├── command/   Command 及解析器（命令 + 选项表 + 裸参数列表）
│   └── dataToken/ TodoToken（内容 + 截止时间 + 重要度）
├── manager/       TodoManager（单例），管理内存列表、排序、对外操作
├── dao/           TodoDao，文件读写（不存在则自动新建）
├── util/          工具箱：时间解析、token 工厂、文件行解析
│   └── handler/    Handler 接口（含共享的序号解析）+ add/query/finish/edit/delete/help 六个实现
└── exception/     异常体系
```

## 设计亮点

- **Handler 模式**：Controller 用 `HashMap<Operator, Handler>` 注册处理器，`dispatch` 逻辑几乎不变，新增指令只需加枚举 + 注册新 Handler，符合开闭原则。
- **异常体系自研**：所有业务异常继承 `AppException`（继承 `RuntimeException`），取消 throw 链条，由 `Input` 主循环统一捕获并调用各自的 `print()` 打印带分类前缀的错误信息；`WrongException` 通过栈回溯自动附带"类名::方法名"定位。
- **防御校验**：`add` 内容为空、`edit` 无修改项/空内容/越界、`query -n` 非正数、`finish`/`delete` 越界均会给出明确错误并继续运行，不写脏数据、不崩溃。
- **已完成事项计数器**：`finishedCount` 字段在 finish 时递增、启动时按 finish.txt 行数初始化，`query -f` 直接打印，不逐次统计。
- **长选项别名**：`Command.OPTION_ALIASES` 集中维护长名→短名映射，解析时归一到短名，handler 仍按短名取值；`-m`、`--message`、`-message` 等效。
- **内置帮助**：`HelpHandler` 用静态表集中描述每个指令的用法与参数，`help` 无参列出全部、带指令名列出参数；错误提示统一引导到 help。
- **复用**：`edit` 复用 `add` 的时间解析（`-d`/`-h`）与 finish 的序号解析（`-n num` / 裸参数 `num`，抽成 `Handler.indexOf`），TodoToken 保持不可变，编辑通过新建替换实现。
- **时间解析宽容**：`TimeUtil.parseTime` 依次尝试"日期+时间 / 仅日期（补 00:00）/ 仅时间（补今天）"三种格式，全部失败才报错。
- **调试日志隔离**：DebugOutput 统一由 `Config.debug` 开关控制，不污染正常输出。
- **每次修改即落盘**：add/finish/edit/delete 后立刻保存文件（finish 同时写 todo.txt 与 finish.txt），退出再兜底 saveAll 一次，数据不易丢失。
- **紧急×重要优先级**：`score = 3×重要度 + 6/max(剩余天数,1)`，紧迫度用反比例（今天/超期封顶 6，1 天 vs 2 天差一倍）；query 把**已逾期任务单独分区**（按 ddl 升序、不计算 score），未逾期按 score 降序、同分按 ddl 升序；默认只显示 `content; ddl`，加 `-d` 才列出 `pri` 与 `score`。
- **一键逾期处理**：`finish -o` / `delete -o` 批量处理所有已逾期任务，避免逐条操作。
- **定宽列对齐**：query 的 `content` 按"终端显示宽度"（CJK/全角按 2 列）左对齐填充到列宽（列宽 = min(本列表最长内容, 24)，超长截断补省略号），序号按最大位数补齐，ddl 列天然对齐，中英文混排也不参差。
- **启动语**：启动时输出"星海系统持续陪伴您，目前已航行xx日xx时"，从 `Config.START_TIME`（2026-04-27）起算，替代原先的指令名罗列。

## 当前状态与未完成项

- 2026-08-12 做过一轮优化：清理死代码（`ErrorUtil`、`FinishedToken`、`Config.invalidToken`、`Command.getOption` 双参重载、冗余 `sort` 调用），规范化命名（`MainClass→Main`、`MyException→AppException`、`allMatter→ALL_PATTERN` 等），输出统一走 `Output`，新增防御校验；同日实现 `edit`、`delete` 指令，finish 改为写入 finish.txt，query 新增 `-f` 查已完成（计数器 + 默认前 10 条）；后又实现 `help` 指令、支持 `--xxx`/`-xxx` 长选项、未知指令提示引导到 help；再实现"重要×紧急"双维度优先级（`-i` 重要度 0~3，反比例紧迫度 score 排序）；query 改为默认隐藏 pri/score、详情参数为 `-d`/`-detail`、字段顺序 content-ddl(-pri-score)，content-ddl 定宽列对齐（CJK 双宽），启动语改为"星海系统苏醒，已航行xx日"；query 再改为已逾期/未逾期分区展示（逾期按 ddl、不计 score），`finish -o`/`delete -o` 一键处理逾期；测试记录见 `out/test/测试记录.md`。
- README v3 中列出的规范要求未全部落地：无 `.gitignore`。
- README 中规划但未做的功能：按标签筛选、批量操作、超期标注等。
- 提交记录显示曾开发"番茄钟"功能，后在 `1a7b432` 删除。
