# 优化后测试记录

- 时间：2026-08-12
- 环境：Java 1.8.0_461，Windows，Git Bash
- 编译：`javac -encoding UTF-8 -d "out/production/todo list" $(find src -name '*.java')`（通过）
- 运行：`java -cp "out/production/todo list" main.Main`，沙盒目录 `out/test-run/` 中复制 `todo.txt` 运行，不污染真实数据

## 用例 1：主流程

| 输入 | 预期 | 实际 |
| --- | --- | --- |
| `query` | 打印 6 条，按 ddl 升序 | ✓ |
| `add -m test-task-a -d 2027-01-01 -h 10:00` | 新增并排到最后 | ✓ |
| `query` | 7 条，新任务在末尾 | ✓ |
| `finish 1` | 删除第 1 条（趣味运动会） | ✓ |
| `query` | 6 条，已删 | ✓ |
| `badcmd` | 报错 `badcmd 指令不存在!`，不崩溃 | ✓ |
| `add -m `（内容为空） | 报错 `add缺少内容, 用法: ...`，不崩溃 | ✓（新增校验） |
| `query -n 2` | 只打印前 2 条 | ✓ |
| `query -n 0` | 报错 `query -n 需要大于0` | ✓（修正：原实现 n<=0 会打印全部） |
| `stop` | 保存并退出，exit=0 | ✓ |

## 用例 2：补充路径

| 输入 | 预期 | 实际 |
| --- | --- | --- |
| `query -a` | 打印全部 | ✓ |
| `finish -n 1` | 删除第 1 条 | ✓ |
| `finish 99` | 报错 `finish索引越界`，不崩溃 | ✓ |
| `query -n 99` | 打印全部（n 超界取列表长度） | ✓ |

## 用例 3：edit 功能（2026-08-12 新增）

初始 6 条，第 1 条为 `趣味运动会`(2026-05-20)，第 2 条为 `os-lab5实验报告`(2026-05-29)。

| 输入 | 预期 | 实际 |
| --- | --- | --- |
| `edit -n 1 -c renamed-one` | 第 1 条内容改为 renamed-one，ddl 不变 | ✓ |
| `edit 2 -d 2026-06-20` | 裸参数序号可用；第 2 条 ddl 改为 2026-06-20，内容不变，重排到靠后 | ✓ |
| `edit -n 1 -c both-change -d 2027-03-03 -h 12:00` | 内容与 ddl 同时改，重排到末尾 | ✓ |
| `edit -n 99 -c x` | 报错 `edit索引越界` | ✓ |
| `edit -n 1` | 报错 `edit至少需要修改内容(-c)或ddl(-d/-h)` | ✓ |
| `edit -n 1 -c `（空内容） | 报错 `edit -c 内容不能为空` | ✓ |
| `edit abc` | 报错 `edit序号不是数字` | ✓ |
| `query` / `stop` | 最终列表按 ddl 升序、落盘正确、exit=0 | ✓ |

## 用例 4：finish→finish.txt、query -f、delete（2026-08-12）

初始 6 条，第 1 条 `趣味运动会`(2026-05-20)，第 2 条 `os-lab5实验报告`(2026-05-29)。

| 输入 | 预期 | 实际 |
| --- | --- | --- |
| `finish 2` | os-lab5 移出 todo、写入 finish.txt | ✓ |
| `query -f` | 开头"已完成事件数：1"，下列该事项 | ✓ |
| `query -f -n 1` / `query -f -a` | 分别前 1 条 / 全部 | ✓ |
| `delete 1` | 删除趣味运动会，不进 finish.txt | ✓ |
| `query -f` | 计数器仍为 1（delete 不影响） | ✓ |
| `finish 99` / `delete 99` | 各自报索引越界 | ✓ |
| `query -f -n 0` | 报错需大于 0 | ✓ |
| todo.txt / finish.txt | 分别为 4 条与 os-lab5 一条 | ✓ |

## 用例 5：query -f 默认前 10 条与计数器恢复（2026-08-12）

add 12 条后连续 finish 12 次：

| 输入 | 预期 | 实际 |
| --- | --- | --- |
| `query -f` | "已完成事件数：12"，仅列前 10 条 | ✓ |
| `query -f -n 3` | 列前 3 条，计数 12 | ✓ |
| `query -f -a` | 列全部 12 条，计数 12 | ✓ |
| 重启后 `query -f` | 计数从 finish.txt 恢复为 12 | ✓ |

## 用例 6：长选项、help、错误提示（2026-08-12）

| 输入 | 预期 | 实际 |
| --- | --- | --- |
| `help` | 列出全部 7 条指令的用法与简介 | ✓ |
| `help add` / `help -add` / `help --add` | 三种写法均列出 add 的参数（-m/-d/-h 及其作用） | ✓ |
| `help query` | 列出 query 参数 | ✓ |
| `help nosuchcmd` | 报错并提示 help | ✓ |
| `badcmd` | 报"指令 badcmd 不存在, 请输入 help 查看可用指令" | ✓ |
| `add --message long-task --date 2026-02-02` | 长选项新增成功，落盘 | ✓ |
| `finish --num 1` | 长选项完成第 1 条 | ✓ |
| `edit 1 -content edited-long` | 单横杠长选项改内容 | ✓ |
| `query --finished` | 长选项查已完成（计数 1） | ✓ |

## 结论

- 编译通过、正常启动，增/查/完成/编辑/删除/退出全流程与原有行为一致；finish 双文件落盘、query -f 默认 10 条 + 计数器均符合要求。
- 长选项（`--xxx`/`-xxx`）经别名归一后与短选项等效；help 与未知指令提示符合要求。
- 新增防御校验（add 空内容、edit/delete 越界、query -n<=0）生效，错误输入不再落盘脏数据。
- 输出文件乱码为控制台/文件编码显示差异（程序读写使用同一默认字符集，往返一致），与本次改动无关。
